# 执行计划：统一 UI 设计令牌与控件基类

## 第一期：令牌层

- [x] 清点高度、间距、焦点环、字号与断点现状。
- [x] 引入角色令牌并以等值 `var()` 替换字面值。
- [x] 审计媒体查询与同断点规则；有可见或级联差异的统一项保留现状并移入第五期。
- [x] 对构建前后 CSS 做解析值与级联顺序复核。
- [x] 运行前端 lint/build、Go test/vet 与本地运行验证。
- [x] 按部署门禁备份并部署到 `10.0.0.6`，验证 systemd、健康接口和嵌入资产。
- [x] 人工验收第一期通过。
- [x] 提交第一期（`574498a`）。

## 后续阶段

- [x] 第二期：药丸基类、显式控件类与 select 外观（`41c57ba`）。
- [x] 第三期：收窄表单选择器并删除由其造成的 `!important`。
- [x] 第四期：统一主题与自动刷新选择交互（本次提交）。

每一期独立完成验证、部署、人工验收与提交；前一期未验收时不开始下一期。

## 暂停点

2026-08-12 完成第四期：主题与自动刷新统一使用可复用的 choice-menu
交互与 `menuitemradio` 语义，主题选项共享数据与基类样式，主题根节点写入
集中到 `PanelApp`；手机端仪表台四项控件满行排列，PC 端立即刷新按钮固定为
圆形。已通过前端 lint/build、Go test/vet、375/390/768/1024/1440 宽度静态
视觉回归和本地运行时检查；按门禁部署到 `10.0.0.6`，最终备份目录为
`/opt/rosboard/backups/rosboard-ui-phase4-fix-20260812-001606/`，远端
systemd、健康/API 合约和嵌入 CSS 均验证通过，用户已人工验收通过。
